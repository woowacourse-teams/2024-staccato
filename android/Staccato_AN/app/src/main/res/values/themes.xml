<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Staccato_AN" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/staccato_blue</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/white</item>
        <!-- Customize your theme here. -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Staccato_AN" parent="Base.Theme.Staccato_AN" />

    <style name="Theme.Staccato_AN.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/icon_staccato_symbol</item>
        <!-- Required for animated icons. -->
        <item name="windowSplashScreenAnimationDuration">200</item>
        <!-- Set the theme of the Activity that directly follows your splash
        screen. This is required. -->
        <item name="postSplashScreenTheme">@style/Theme.Staccato_AN</item>
    </style>

    <style name="ThemeOverlay.NumberPicker" parent="Theme.Staccato_AN">
        <item name="colorControlNormal">@color/gray2</item>
    </style>
</resources>
